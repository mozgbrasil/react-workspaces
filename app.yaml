runtime: nodejs
env: flex

env_variables:
  NODE_VERBOSE: true
  GOOGLE_CLIENT_ID: 390000000004-390000000004.apps.googleusercontent.com
  GOOGLE_CLIENT_SECRET: 390000000004-390000000004-
  GOOGLE_REDIRECT_URL: https://390000000004.appspot.com/390000000004/oauthCallback
  COMMAND_BUILD: yarn workspace @mozg/react-workspace run build
  COMMAND_START: yarn workspace @mozg/react-workspace run server:node

# This sample incurs costs to run on the App Engine flexible environment.
# The settings below are to reduce costs during testing and are not appropriate
# for production use. For more information, see:
# https://cloud.google.com/appengine/docs/flexible/nodejs/configuring-your-app-with-app-yaml
manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
